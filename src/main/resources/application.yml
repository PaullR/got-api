feign:
  client:
    config:
      bookFeignClient:
        loggerLevel: FULL

client:
    baseUrl: https://www.anapioficeandfire.com/api

logging:
  level:
    root: INFO
    com.got.gotapi.feign: iNFO